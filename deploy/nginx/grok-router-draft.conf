# Nginx single-entry with Grok routing (draft fragment) — G7-P1
# ============================================================
# 草案：合并到 main 前需人工 review。G7-P1 待启用（需 grok2api-rs 挂载 /v1 路由后按 model 分流）。
# 此段为 /etc/nginx/conf.d/tnexus.relai.asia.conf 的摘录式草案，非独立可加载文件。

server {
    listen 443 ssl;
    server_name tnexus.relai.asia;

    # ── Grok Admin（可选，部署后启用）────────────────────────────────
    # G7-P1 待启用：/admin/grok/* → grok-admin :8091
    # location /admin/grok/ {
    #     proxy_pass http://127.0.0.1:8091;
    #     proxy_http_version 1.1;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    #     proxy_read_timeout 120s;
    # }

    # ── OpenAI 兼容推理入口 ──────────────────────────────────────────
    # G7-P1 待启用：Grok-RESPONSES 数据面专用路径显式走 :8000，
    # 其余 /v1/*（生图/对话）保持现有 :8014。真实按 model=grok-* 分流
    # 需 grok2api-rs 挂载 /v1 路由（N5）后在 body 判断或按路由前缀启用。
    # location ~ ^/v1/(responses|videos)($|/) {
    #     proxy_pass http://127.0.0.1:8000;
    #     proxy_http_version 1.1;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    #     proxy_read_timeout 1200s;
    #     proxy_buffering off;
    # }

    # ── 现有：ChatGPT gateway（保持不动）────────────────────────────
    # location /v1/ {
    #     proxy_pass http://127.0.0.1:8014;
    #     ...
    # }

    # ── 现有：TNexus 控制面 API + 静态 UI（保持不动）─────────────────
    # location / {
    #     proxy_pass http://127.0.0.1:9000;
    #     ...
    # }
}