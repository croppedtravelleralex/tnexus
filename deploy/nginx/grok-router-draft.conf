# Nginx single-entry with Grok routing (draft fragment) — G7-P1
# ============================================================
# 草案：合并到 main 前需人工 review。G7-P1 待启用（:8000 已挂载 /v1 全路由）。
# 此段为 /etc/nginx/conf.d/tnexus.relai.asia.conf 的摘录式草案，非独立可加载文件。

server {
    listen 443 ssl;
    server_name tnexus.relai.asia;

    # ── Grok Admin（grok2api-rs 内嵌 :8091，仅 127.0.0.1，启用后打开）──
    # 前端 /grok/* 页面走此反代：生产需同时设 NEXT_PUBLIC_GROK_ADMIN_BASE=/admin/grok/。
    # location /admin/grok/ {
    #     proxy_pass http://127.0.0.1:8091;
    #     proxy_http_version 1.1;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    #     proxy_read_timeout 120s;
    # }

    # ── OpenAI 兼容推理入口 ──────────────────────────────────────────
    # 首轮切流范围（G6-P3 判定）：chat/completions + responses + messages → :8000；
    # 生图 /media /videos 在 :8000 未接线（500/501），保持 :8014，待生图接线后按 model 分流。
    # location ~ ^/v1/(chat/completions|responses|messages)($|/) {
    #     proxy_pass http://127.0.0.1:8000;
    #     proxy_http_version 1.1;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    #     proxy_read_timeout 1200s;
    #     proxy_buffering off;
    # }

    # ── 现有：ChatGPT gateway（保持不动；生图/媒体/视频仍走 :8014）──
    # location /v1/ {
    #     proxy_pass http://127.0.0.1:8014;
    #     ...
    # }

    # ── 现有：TNexus 控制面 API + 静态 UI（保持不动）─────────────────
    # location / {
    #     proxy_pass http://127.0.0.1:9000;
    #     ...
    # }
}